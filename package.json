{
  "name": "rer-reader",
  "private": true,
  "version": "1.6.2",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "python3 scripts/build_userscript.py && python3 extension/build_extension.py",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.10",
    "typescript": "5.9.2"
  }
}
